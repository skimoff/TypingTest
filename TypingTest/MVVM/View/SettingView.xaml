<UserControl x:Class="TypingTest.MVVM.View.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TypingTest.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Border Margin="20"
            CornerRadius="10"
            Style="{StaticResource UserControlsBorderStyle}">
        <StackPanel Orientation="Vertical"
                    Margin="20">
            <TextBlock Text="Theme"
                     FontSize="20"
                     Foreground="#F2F2F2"/>
            <StackPanel Orientation="Horizontal">
                <RadioButton Height="25"
                             Width="100"
                             Content="Dark"
                             IsChecked="True"
                             Style="{StaticResource SettingRadioButton}"/>
                <RadioButton Height="25"
                             Width="100"
                             Content="Light"
                             Style="{StaticResource SettingRadioButton}"/>
            </StackPanel>
            
            <TextBlock Text="Language"
                       FontSize="20"
                       Foreground="#F2F2F2"/>
            <StackPanel Orientation="Horizontal">
                <RadioButton Height="25"
                             Width="100"
                             Content="UA"
                             IsChecked="{Binding IsUkrSelected, Mode=TwoWay}"
                             Style="{StaticResource SettingRadioButton}"/>
                <RadioButton Height="25"
                             Width="100"
                             Content="ENG"
                             IsChecked="{Binding IsEngSelected, Mode=TwoWay}"
                             Style="{StaticResource SettingRadioButton}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
